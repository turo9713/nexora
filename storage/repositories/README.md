# Repositories

The active v1.4 JSON repositories remain under
`integrations/telegram_runtime/storage`. The SQLite mirror is implemented in
`database/repository` to avoid duplicate import names during the transition.
