# Agent definitions

Nexora preserves the existing, runtime-compatible manifests at
`agents/<agent-id>/agent.yaml`. The registry loads those eight manifests as the
single source of truth; this directory intentionally contains no duplicate
definitions.
