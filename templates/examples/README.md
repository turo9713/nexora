# Workflow template examples

Templates are declarative YAML documents. They select existing agents and skills but never contain executable code, environment values, secrets, or commands.

Use the built-in manifests as reviewed examples. Community submissions stay outside the active registry until validation and security review succeed.
