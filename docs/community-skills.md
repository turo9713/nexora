# Community skills

Community skills are submitted separately from the active registry. Required artifacts are a manifest, permissions, tests, documentation, and a security review.

The flow is `Submit -> Validation -> Security Check -> Approved -> Available`. Validation is fail-closed; packages cannot request shell, root, Docker, secret, or production access and are never downloaded or activated automatically.
