id: threat-modeling
name: Threat Modeling
version: 1.0.0
description: Моделирование угроз по STRIDE, анализ границ доверия и подготовка безопасных мер снижения риска.
author: Nexora
category: development
agent: developer
permissions:
  filesystem:
    scope: [workspace]
    mode: read_only
  network:
    mode: none
  shell:
    enabled: false
  production:
    enabled: false
risk:
  level: low
tools: [repository_analysis, reporting]
security:
  sandbox:
    enabled: true
approval:
  required: false
compatibility:
  min_platform: 1.7.0
status: DISABLED
