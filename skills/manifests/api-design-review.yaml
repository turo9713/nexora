id: api-design-review
name: API Design Review
version: 1.0.0
description: Read-only анализ API-контрактов, версионирования, пагинации и безопасной обработки ошибок.
author: Nexora
category: development
agent: developer
permissions:
  filesystem:
    scope: [workspace]
    mode: read_only
  network:
    mode: none
  shell:
    enabled: false
  production:
    enabled: false
risk:
  level: low
tools: [repository_analysis, reporting]
security:
  sandbox:
    enabled: true
approval:
  required: false
compatibility:
  min_platform: 1.7.0
status: DISABLED
