# Policy engine

Every registered agent is denied by default. The engine validates agent status,
allowed tools, workspace scope, risk, forbidden action types, and approval state
before execution. Critical and security-control changes are always denied.
