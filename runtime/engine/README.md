# Runtime engine compatibility layer

Nexora v1.5 does not move or rewrite the v1.4 runtime modules. New platform
services are composed around the existing engine and can be migrated here in a
future major version.
